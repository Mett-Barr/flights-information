<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".FlightInformationApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FlightInformation">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.FlightInformation">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Deep link：flightinformation://flights 與 flightinformation://currency。
                 scheme 與 DEEP_LINK_SCHEME 一致，host 的解析見 NavDeepLink.kt。 -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <!-- Android 會對同一個 intent-filter 內所有 data 屬性做交叉組合，
                     拆開寫語意相同但不會誤導（lint: IntentFilterUniqueDataAttributes）。 -->
                <data android:scheme="flightinformation" />
                <data android:host="flights" />
                <data android:host="currency" />
            </intent-filter>
        </activity>
    </application>

</manifest>
